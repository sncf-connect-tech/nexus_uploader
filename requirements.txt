# pip-tools currently has no release supporting pip 10 (pending https://github.com/jazzband/pip-tools/commit/4e872f1)
# hence we do not support it:
pip>=8.1.1,<=9.0.3
pip-tools>=1.10.0,<=1.11.0
requests>=2.8.1
